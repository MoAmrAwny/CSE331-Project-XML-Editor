#include "Minifying.h"

int main() {
    string fileName = "sample (1).xml"; // Specify the XML file to minify
    minifying(fileName);               // Call the function to process the file
    return 0;
}
