#include "Minifying.h"

int main() {
    string fileName = "minified.xml"; // Specify the XML file to minify
    string output= "sample (1).xml" ;
    minifying(fileName,output);               // Call the function to process the file
    return 0;
}
